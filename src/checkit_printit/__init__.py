"""checkit-printit: turn a CheckIt bank and a roster into a printable class set.

Companion to `checkit-dashboard`, kept separate because rosters, seating charts
and Google accounts are not platform concerns.

Design notes live in `PRINT_TOOL_DESIGN.md` in the checkit repo.
"""

__version__ = "0.2.0"
